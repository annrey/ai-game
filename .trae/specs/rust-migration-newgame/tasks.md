# Tasks

- [x] Task 1: 创建 `newgame` 目录并初始化基础 Rust 工作区。
  - [x] SubTask 1.1: 在项目根目录创建 `newgame` 文件夹。
  - [x] SubTask 1.2: 在 `newgame` 下创建 `Cargo.toml` 并配置为 Cargo Workspace。
  - [x] SubTask 1.3: 根据之前的迁移方案规划，在 `newgame` 下创建 `apps`、`crates` 和 `ffi` 等基础子目录。

# Task Dependencies
无依赖，按顺序执行即可。
