- [x] 游戏引擎 (`game-engine.ts`) 支持处理 `<WAIT>` 输入，并将其识别为时间流逝。
- [x] 闲置时的叙事输出合理地描写了环境或 NPC 变化，而不是催促玩家输入。
- [x] 前端 UI (`ui/index.html`) 支持闲置检测机制，能自动触发世界演化（发往后端的请求）。
- [x] 自动世界演化机制具备并发保护，不会在等待本地大模型返回时发送新的请求导致服务拥塞。
- [x] 前端设置面板新增了控制自动演化（Auto-Tick）的开关和时长设置。
- [x] 在玩家未进行操作指定时间后，UI 会自动更新新的剧情段落和状态。