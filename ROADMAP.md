# OpenClaw Game 发展规划 (ROADMAP)

## 1. 现状及单体架构痛点
当前项目采用单体架构，虽然在初期开发中能够快速迭代并验证核心逻辑，但随着功能（如多 Agent、记忆系统、引擎逻辑、多种 Provider）的不断增加，暴露出了以下痛点：
- **代码耦合度高**：前端 UI、后端 Server、核心引擎、记忆存储以及各类 Agent 的逻辑紧密交织，难以独立维护和测试。
- **扩展性受限**：新增自定义的 Provider、Skill 或 Game Mode 往往需要修改核心代码，缺乏统一且解耦的扩展机制。
- **依赖管理混乱**：所有模块共用一个 `package.json`，导致依赖冗余，版本冲突风险增加，构建时间变长。
- **协同开发困难**：随着项目规模扩大，不同方向（如前端、后端、AI 模型对接）的开发者在同一代码库下容易产生冲突。

## 2. 短期规划 (Phase 1)
**目标**：Monorepo 重构与插件化核心（Plugin SDK）
- **架构升级**：引入 pnpm workspace，将项目重构为 Monorepo 结构，拆分基础包（如 `@openclaw/core`, `@openclaw/shared-types`, `@openclaw/memory`）与应用层（`apps/server`, `apps/ui`）。
- **插件化改造 (Plugin SDK)**：设计并实现核心扩展机制，定义统一的 `GameExtension` 接口。支持 Provider、Game Mode 和 Skill 以插件形式动态加载，实现核心逻辑与扩展模块的解耦。
- **依赖与构建优化**：重塑各子模块的依赖关系与构建流程，确保 `build`, `dev`, `test` 能够独立或协同运行。

## 3. 中期规划 (Phase 2)
**目标**：模组生态（Modding）、长期记忆深度演进、多模态互动
- **模组生态 (Modding Ecosystem)**：基于 Plugin SDK 开放标准的 Mod 开发规范，允许社区开发者提供自定义的剧情、角色、规则以及 AI Prompt。
- **长期记忆深度演进**：引入向量数据库或图数据库支持，增强 Agent 对历史事件的总结、反思和提取能力，实现真正具有连贯成长性的长期记忆。
- **多模态互动**：
  - **语音交互**：接入 TTS (Text-to-Speech) 与 STT (Speech-to-Text) 能力，赋予 Agent 真实感的声音输出与语音指令接收。
  - **图像生成**：集成生图模型，根据场景和剧情动态生成游戏内插画或角色立绘。

## 4. 远期规划 (Phase 3)
**目标**：多玩家联机跑团、完全开放世界模拟器、高级推理架构
- **多玩家联机跑团 (Multiplayer TRPG)**：支持多名真人玩家在同一虚拟世界中互动，由 AI 担任“跑团主持 (DM)”及各类 NPC。
- **完全开放世界模拟器**：打造具备自运行规律的虚拟社会体系，NPC 拥有各自的生活轨迹、经济系统及社会关系网，即使没有玩家介入也能自我演化。
- **高级推理架构**：引入多 Agent 辩论决策（Debate & Consensus）、思维树（ToT）等复杂认知架构，使 AI 角色能够处理更加复杂的逻辑谜题和深度人际关系。
