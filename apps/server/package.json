{
  "name": "@openclaw/server",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsup src/server.ts --format esm --dts",
    "start": "node dist/server.js"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^16.4.5",
    "express": "^5.2.1",
    "zod": "^3.23.8",
    "@openclaw/core": "workspace:*",
    "@openclaw/shared-types": "workspace:*",
    "@openclaw/memory": "workspace:*",
    "@openclaw/provider-local": "workspace:*"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.6",
    "tsx": "^4.19.0"
  }
}
